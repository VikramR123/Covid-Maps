Use "expo start" to run and then hit "i" to start iOS simulator (Need XCode)

Notes:

Hooks are ways to maintain State in function components (as opposed to class components which I am not using)

Using Firebase DB